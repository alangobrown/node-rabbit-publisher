{
  "name": "send-to-rabbit",
  "private": "true",
  "version": "0.0.1",
  "description": "simple node.js app to write a message to rabbit mq",
  "scripts": {
    "start": "node send.js"
  },
  "dependencies": {
    "amqplib": "^0.3.1",
    "when": "^3.7.2"
  }
}
